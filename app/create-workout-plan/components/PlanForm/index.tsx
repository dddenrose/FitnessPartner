"use client";
import FormAction from "../FormAction";
import FormContent from "../FormContent";

const PlanForm = () => {
  return (
    <FormAction>
      <FormContent />
    </FormAction>
  );
};

export default PlanForm;
